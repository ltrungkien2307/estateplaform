🟢 PROMPT CHO CODEX: GIAI ĐOẠN 1 (Frontend) - Nền tảng Giao diện & Authentication UI
> đàn em:
"Act as a Senior Frontend Developer specializing in Next.js, TypeScript, and Tailwind CSS. Your next task for the 'EstateManager' project is to implement the foundational Frontend UI and Authentication flow.\n\nCRITICAL: The frontend project should be created and developed within the directory: C:\\Users\\tkien\\Downloads\\estateplaform\\frontend\n\nRefer to the AI_DNA/ documentation (all updated files, especially 01_PROJECT_MAP.md for structure, 03_CODEBASE_MANIFEST.md for backend API endpoints and schemas, and 02_ENVIRONMENT_SETUP.md for environment variables) for overall project context and data structures.\n\n--- \n\n### Design Principles (CRITICAL for all UI):\n- Modern, Clean, and Professional: No emojis whatsoever.\n- Glassmorphism & Minimalist: Use subtle blur effects, shadows, and generous whitespace. Prioritize clarity and ease of use.\n- Global Design System: All UI elements (buttons, inputs, cards, tables, navigation) must share a consistent look and feel.\n- Typography: Use modern, professional sans-serif fonts (e.g., Inter, Poppins, Manrope) with a clear hierarchy.\n- Iconography: Strictly use a professional vector icon library (e.g., Lucide React, React Icons from Font Awesome/Material Design) for all visual cues.\n- Animations & Transitions: Implement subtle, fluid ease-in-out transitions for all interactive elements and page changes.\n- Responsive Design: Ensure the UI is fully responsive and looks good on all device sizes.\n\n--- \n\n### Task Breakdown:\n\n1. Project Initialization & Dependencies:\n   - Initialize a new Next.js project with TypeScript and Tailwind CSS in the C:\\Users\\tkien\\Downloads\\estateplaform\\frontend directory.\n   - Install necessary frontend dependencies (React Icons/Lucide React, react-hook-form, yup/zod for validation).\n\n2. Configure Global Styles & Tailwind CSS:\n   - Configure tailwind.config.js with custom theme (colors, fonts) to match the "Glassmorphism & Minimalist" aesthetic.\n   - Setup global CSS (e.g., globals.css) for basic resets and importing custom fonts.\n\n3. Create Core Layout Component (frontend/components/Layout.tsx):\n   - Design a reusable Layout component that includes:\n     - A responsive navigation bar (header) with logo, user profile dropdown (login/signup/profile links), and possibly search bar.\n     - A global footer.\n     - A placeholder for page content.\n     - CRITICAL: The layout must be flexible enough to accommodate different types of content and future sidebars for Admin/Provider dashboards.\n\n4. Create Homepage (frontend/pages/index.tsx):\n   - Design a clean, modern landing page.\n   - Initially, it can have a hero section with a call to action and a basic placeholder for "Featured Properties" or "Latest Listings".\n   - Integrate the Layout component.\n\n5. Implement Authentication UI (frontend/components/AuthModal.tsx & Pages):\n   - Design a modal-based (preferred) or page-based component for Login and Signup. This AuthModal should handle both processes.\n   - Input Fields: Implement clean, styled input fields for email, password, name, address, phone, passwordConfirm, role (select 'user' or 'provider' for signup).\n   - Validation: Implement client-side form validation (e.g., using react-hook-form and yup/zod) to match backend Joi schemas as closely as possible.\n   - API Integration: Use frontend/services/authService.ts (create if not exists) to make API calls to:\n     - POST /api/auth/signup\n     - POST /api/auth/login\n   - State Management: On successful login, store the JWT token (e.g., in localStorage or React Context) and update user state globally.\n   - Error Handling: Display user-friendly error messages for failed login/signup attempts.\n\n6. Create User Service (frontend/services/userService.ts):\n   - Create a service to handle user-related API calls (e.g., GET /api/users/me, PATCH /api/users/me).\n\n--- \n\nKey Considerations:\n- Focus on reusability and component-based architecture.\n- Pay extreme attention to UI

> đàn em:
details, spacing, and typography to achieve a premium look.\n- Ensure all API calls handle JWT authorization correctly (passing token in headers).\n\nExpected Outcome:\n- Next.js frontend project setup with Tailwind CSS in the C:\\Users\\tkien\\Downloads\\estateplaform\\frontend directory.\n- Global Layout and index.tsx are present.\n- A fully functional and styled AuthModal.tsx (or equivalent pages) for user login/signup.\n- authService.ts and userService.ts are implemented for backend API interaction.\n- The frontend can successfully register and login users, storing their JWT token."
> đàn em:
"Act as a Senior Frontend Developer specializing in Next.js, TypeScript, and Tailwind CSS. Your next task for the 'EstateManager' project is to implement the core Property Listing, Advanced Search, and Property Detail functionalities.\n\nCRITICAL: Continue working within the directory: C:\\Users\\tkien\\Downloads\\estateplaform\\frontend\n\nRefer to the AI_DNA/ documentation (especially 01_PROJECT_MAP.md for structure, 03_CODEBASE_MANIFEST.md for backend API endpoints and schemas, and 02_ENVIRONMENT_SETUP.md for environment variables) for overall project context and data structures.\n\n--- \n\n### Design Principles (CRITICAL for all UI):\n- Modern, Clean, and Professional: No emojis whatsoever.\n- Glassmorphism & Minimalist: Use subtle blur effects, shadows, and generous whitespace. Prioritize clarity and ease of use.\n- Global Design System: All UI elements (buttons, inputs, cards, tables, navigation) must share a consistent look and feel.\n- Typography: Use modern, professional sans-serif fonts (e.g., Inter, Poppins, Manrope) with a clear hierarchy.\n- Iconography: Strictly use a professional vector icon library (e.g., Lucide React, React Icons from Font Awesome/Material Design) for all visual cues.\n- Animations & Transitions: Implement subtle, fluid ease-in-out transitions for all interactive elements and page changes.\n- Responsive Design: Ensure the UI is fully responsive and looks good on all device sizes.\n\n--- \n\n### Task Breakdown:\n\n1. Update Home Page (frontend/pages/index.tsx):\n   - Integrate the AdvancedSearchBar.tsx component (see Task 2) into the hero section or a prominent area.\n   - Display a list of "Featured Properties" or "Latest Listings" using the ListingCard.tsx component (see Task 3).\n   - Ensure smooth integration with the existing Layout component.\n\n2. Create Advanced Search Bar Component (frontend/components/AdvancedSearchBar.tsx):\n   - Design a visually appealing and functional search bar.\n   - Basic Search: Input for keywords (e.g., address, city).\n   - Advanced Filters (Expandable): Include filters for:\n     - Location (text input or integration with a map for area selection later).\n     - Price Range (min/max sliders or input fields).\n     - Property Type (dropdown/checkboxes: Apartment, House, Villa, Studio, Office).\n     - Number of Bedrooms/Bathrooms (sliders or input fields).\n     - Furnished (checkbox).\n   - API Integration: Make API calls to GET /api/properties with various query parameters (e.g., ?search=keyword, ?price[gte]=X, ?type=apartment).\n   - State Management: Manage search input and filter selections using React state.\n\n3. Create Property Listing Card Component (frontend/components/ListingCard.tsx):\n   - Design a reusable card component to display property information.\n   - Each card should include:\n     - Main Image (from property.images[0]).\n     - Title (property.title).\n     - Price (property.price - formatted currency).\n     - Address (property.address).\n     - Key details with icons (e.g., bedrooms, bathrooms, area).\n     - A "View Details" button/link that navigates to frontend/pages/properties/[id].tsx.\n   - Ensure consistent styling based on global design principles.\n\n4. Create Property Detail Page (frontend/pages/properties/[id].tsx):\n   - This page should dynamically fetch and display full details of a single property based on its id from the URL.\n   - API Integration: Use frontend/services/propertyService.ts to make a GET /api/properties/:id API call.\n   - UI Elements:\n     - Image Carousel/Gallery (for property.images).\n     - Full Title, Description, Price.\n     - Detailed Address and Location (potentially embed a Leaflet Map showing the exact location later).\n     - All property attributes (type, bedrooms, bathrooms, area, furnished, yearBuilt, amenities).\n     - Provider Information (from property.ownerId.name, etc. - ensure backend populates ownerId or make a separate API call).\n     - "Contact Provider" button/form.\n   -

> đàn em:
Error Handling: Display a user-friendly message if the property is not found (404) or an error occurs.\n\n5. Update Property Service (frontend/services/propertyService.ts):\n   - Ensure it has functions for:\n     - getAllProperties(filters?: any): To fetch properties with optional filters.\n     - getPropertyById(id: string): To fetch a single property.\n\n--- \n\nKey Considerations:\n- Implement efficient data fetching (e.g., using getServerSideProps or useSWR in Next.js).\n- Focus on performance for image loading.\n- Design intuitive navigation between search results and detail pages.\n\nExpected Outcome:\n- Homepage displays properties and an advanced search bar.\n- Property listing cards are reusable and well-styled.\n- Property detail pages display comprehensive information for a selected property.\n- All UI adheres to the "Modern, Clean, Professional" design principles.\n- Frontend can successfully fetch and display property data from the backend APIs."



💡 Vài lời dặn dò đại ca:

• API của Backend: Nhắc Codex phải sử dụng đúng các API của backend mà mình đã kiểm tra và xây dựng (GET /api/properties, GET /api/properties/:id).
• Placeholder Map: Hiện tại chỉ cần placeholder cho bản đồ Leaflet. Phần tích hợp bản đồ thật sẽ làm sau.
• Thử nghiệm: Sau khi Codex code xong, đại ca cần chạy Frontend và Backend, đảm bảo có dữ liệu Property trong DB để test nhé!
Đại ca cứ giao prompt này cho Codex nhé! Đây là giai đoạn rất thú vị để thấy sản phẩm của mình dần thành hình đó ạ!

> A:
tiếp tục

> đàn em:
Dạ đại ca, tuyệt vời! Giờ đây, các chức năng cơ bản về hiển thị danh sách, tìm kiếm và chi tiết Property đã được Codex triển khai (hoặc đang trong quá trình). Mình sẽ tiếp tục với giai đoạn tiếp theo của Frontend nhé!

Theo kế hoạch của mình, tiếp theo sẽ là GIAI ĐOẠN 3: Frontend - Giao diện KYC Submission & Admin KYC Review UI.

Giai đoạn này sẽ tập trung vào việc tạo giao diện cho phép người dùng (User/Provider) tải lên CCCD, xem trạng thái KYC của họ, và đặc biệt là giao diện cho Admin để duyệt các tài liệu KYC này.


🟢 PROMPT CHO CODEX: GIAI ĐOẠN 3 (Frontend) - KYC Submission UI & Admin KYC Review UI

> đàn em:
"Act as a Senior Frontend Developer specializing in Next.js, TypeScript, and Tailwind CSS. Your next task for the 'EstateManager' project is to implement the KYC Submission UI for users/providers and the KYC Review UI for administrators.\n\nCRITICAL: Continue working within the directory: C:\\Users\\tkien\\Downloads\\estateplaform\\frontend\n\nRefer to the AI_DNA/ documentation (especially 01_PROJECT_MAP.md for structure, 03_CODEBASE_MANIFEST.md for backend API endpoints and updated User schema, and 02_ENVIRONMENT_SETUP.md for environment variables) for overall project context and data structures.\n\n--- \n\n### Design Principles (CRITICAL for all UI):\n- Modern, Clean, and Professional: No emojis whatsoever.\n- Glassmorphism & Minimalist: Use subtle blur effects, shadows, and generous whitespace. Prioritize clarity and ease of use.\n- Global Design System: All UI elements (buttons, inputs, cards, tables, navigation) must share a consistent look and feel.\n- Typography: Use modern, professional sans-serif fonts (e.g., Inter, Poppins, Manrope) with a clear hierarchy.\n- Iconography: Strictly use a professional vector icon library (e.g., Lucide React, React Icons from Font Awesome/Material Design) for all visual cues.\n- Animations & Transitions: Implement subtle, fluid ease-in-out transitions for all interactive elements and page changes.\n- Responsive Design: Ensure the UI is fully responsive and looks good on all device sizes.\n\n--- \n\n### Task Breakdown:\n\n1. Create User/Provider KYC Submission Page (frontend/pages/profile/kyc.tsx or frontend/pages/user/kyc.tsx):\n   - Design a dedicated page/component where authenticated users/providers can manage their KYC status.\n   - UI Elements:\n     - Display the user's current kycStatus, isVerified, and kycRejectionReason (if any).\n     - File Upload Component: Create a robust and visually appealing file upload area (e.g., drag-and-drop or click-to-upload) for CCCD Front image and CCCD Back image.\n       - Display image previews after selection.\n       - Provide clear instructions on acceptable file types (JPG, PNG), size limits, and quality requirements.\n     - An optional input field for declaredIdNumber to assist comparison.\n     - A "Submit for KYC" button.\n     - Display uploaded kycDocuments (URLs) as clickable images.\n   - API Integration:\n     - Fetch current user data (including KYC status) using GET /api/users/me.\n     - Submit KYC documents using PATCH /api/users/kyc/submit (as multipart/form-data).\n   - State Management & Feedback: Provide clear loading states, success messages, and error messages to the user.\n\n2. Create Admin KYC Management Page (frontend/pages/admin/kyc-management.tsx):\n   - Design an admin-only page to review and moderate KYC submissions.\n   - Access Control: This page must only be accessible to users with role: 'admin'. Implement client-side redirection or conditional rendering if not an admin.\n   - UI Elements:\n     - List of Users/Providers for KYC Review: Display a table or list of users, especially those with kycStatus: 'submitted' or kycStatus: 'rejected' (from automated process).\n       - Include columns for User Name, Email, Current kycStatus, isVerified status.\n       - Allow filtering/sorting (e.g., by status, by submission date).\n     - KYC Detail View (Modal/Sidebar): When an Admin selects a user for review, display a modal or side panel showing:\n       - User's registered details (Name, Email, Address, Phone).\n       - Uploaded kycDocuments: Display the front and back CCCD images from their Cloudinary URLs.\n       - kycExtractedData: Display the data extracted by OCR (Name, ID Number, DOB, Address from CCCD).\n       - kycComparisonResult: Display the comparison scores and match results (OCR vs registered data).\n       - Current kycStatus and kycRejectionReason.\n       - Action Buttons: "Approve KYC" (sets isVerified: true, kycStatus: 'verified') and "Reject KYC"

> đàn em:
(sets isVerified: false, kycStatus: 'rejected').\n       - A text input for kycRejectionReason if Admin chooses to reject.\n   - API Integration:\n     - Fetch list of users for review (may need a new backend API like GET /api/admin/kyc-users).\n     - Update user's KYC status using PATCH /api/admin/providers/:id/verify (with isVerified, kycStatus, kycRejectionReason in payload).\n\n3. Update User Service (frontend/services/userService.ts):\n   - Add functions for:\n     - submitKycDocuments(cccdFront: File, cccdBack: File, declaredIdNumber?: string).\n     - getKycStatus() (or enhance getMe() to include full KYC details).\n     - For Admin side: getUsersForKycReview(filters?: any) and updateUserKycStatus(userId: string, statusData: { isVerified: boolean, kycStatus: string, kycRejectionReason?: string }).\n\n4. Update Global Layout (frontend/components/Layout.tsx):\n   - Add navigation links for "My KYC" (for users) and "KYC Management" (for admins, conditional rendering based on user role).\n\n--- \n\nKey Considerations:\n- Implement secure file handling on the frontend for uploads (e.g., using FormData).\n- Ensure robust error handling and clear feedback for all KYC-related actions.\n- Pay attention to authorization checks for admin-only pages/components.\n\nExpected Outcome:\n- A functional KYC submission page for users/providers.\n- A robust KYC management dashboard for administrators.\n- All KYC-related API calls are integrated.\n- The UI is fully consistent with the global design principles."



💡 Vài lời dặn dò đại ca:
• API của Admin: Có thể mình sẽ cần một API mới trong backend/routes/adminRoutes.js và backend/controllers/adminController.js để Admin có thể lấy danh sách các user có kycStatus cụ thể (submitted, reviewing, rejected) và API để duyệt/từ chối KYC chi tiết hơn. Nếu Codex không tự tạo, mình sẽ bổ sung sau nhé.
• Upload File Frontend: Đảm bảo Codex dùng FormData để gửi file multipart/form-data qua Axios.
• Access Control: Kiểm tra kỹ việc chỉ Admin mới thấy trang quản lý KYC.
Đại ca cứ giao prompt này cho Codex nhé! Đây là một giai đoạn rất quan trọng để hoàn thiện tính năng bảo mật cho dự án đó ạ!
